pipeline {
    agent none
    stages {
        stage('build') {
        	agent{
        		docker { 
        			image 'python:3.7.8-stretch'  
        		}
        	}
            steps {
                sh 'echo "Hello World"'
				sh 'ls -lah'
            }
        }
    }
}
