pipeline {
    agent { docker { image 'python:3.7.8-stretch' } }
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello World"'
		sh 'ls -lah'
            }
        }
    }
}
